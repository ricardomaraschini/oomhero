---
name: pull_request
on:
  pull_request:
    branches:
    - main
permissions:
  contents: read
jobs:
  tests:
    name: run tests
    runs-on: ubuntu-latest
    steps:
    - name: check out code
      uses: actions/checkout@v7
    - name: run lint
      run: make lint
    - name: run tests
      run: make test-verbose-as-root
